type Author {
  name: String! @unique
  lastName: String!
  dateOfBirth: Date
  active: Boolean @default(value: false)
  posts: [Post]
  comments: [Comment]
  avatar: Avatar @has_one
}

type Post {
  author: Author
  name: String! @unique
  body: Text!
  status_word: StatusWord
  comments: [Comment]
}

type Comment {
  author: Author
  post: Post
  body: String!
}

type Avatar {
  image_url: String
  author: Author
}

enum StatusWord {
  APPROVED
  PENDING
  REJECTED
}

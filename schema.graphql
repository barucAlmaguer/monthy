type Author {
  name: String!
  lastName: String!
  dateOfBirth: Date
  active: Boolean @default(value: false)
  posts: [Post]
  comments: [Comment]
}

type Post {
  author: Author
  name: String!
  body: String!
  status_word: StatusWord
  comments: [Comment]
}

type Comment {
  author: Author
  post: Post
  body: String!
}

enum StatusWord {
  APPROVED
  PENDING
  REJECTED
}
